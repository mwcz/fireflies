{
  "name": "fireflies",
  "version": "1.0.0",
  "description": "A particle engine using image masks.",
  "main": "index.js",
  "scripts": {
    "test": "npm run build",
    "start": "browser-sync start --server --serveStatic src --files 'src/**/*' --no-inject-changes --directory",
    "build": "babel src --out-dir dist --copy-files --presets=env && for f in dist/js/*.js; do uglifyjs -c -o $f $f; done"
  },
  "author": "mwcz <mwc@clayto.com>",
  "license": "GPL-3.0",
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-env": "^1.1.6",
    "browser-sync": "^2.18.2"
  }
}
